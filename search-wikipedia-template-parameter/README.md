# Search Wikipedia Template Parameter

Search for Wikipedia pages which use `template_name` and don't use `parameter_name`.

Gives false positive may be also false negatives, can't parse wiki template correctly