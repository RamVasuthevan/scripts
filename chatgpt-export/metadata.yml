databases:
  export:
    tables:
      conversations:
        facets:
          - {"array": "safe_urls"}
          - {"array": "moderation_results"}
          - current_node
          - plugin_ids
          - gizmo_id
          - is_archived
          - default_model_slug
          - conversation_origin
          - voice
          - async_status
